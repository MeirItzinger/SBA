# Database (Neon Postgres)
DATABASE_URL="postgresql://user:password@ep-xxxxx.us-east-2.aws.neon.tech/neondb?sslmode=require"

# OpenAI API Key (optional - mock responses will be used if not set)
OPENAI_API_KEY=""

# Storage driver: "local" for development, "vercel-blob" for production
STORAGE_DRIVER="local"

# Vercel Blob (only needed if STORAGE_DRIVER="vercel-blob")
BLOB_READ_WRITE_TOKEN=""

# Local storage path (for development)
LOCAL_STORAGE_PATH="./uploads"

